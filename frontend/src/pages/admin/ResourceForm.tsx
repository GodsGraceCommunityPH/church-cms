import{useEffect,useState}from'react';import{useNavigate,useParams}from'react-router-dom';import Button from'../../components/ui/Button';import Input from'../../components/ui/Input';import Select from'../../components/ui/Select';import Textarea from'../../components/ui/Textarea';import{ACCEPTED_RESOURCE_TYPES,ALLOWED_EXTENSIONS,CATEGORY_LABELS,type ResourceCategory}from'../../features/resources/resource';import{createResource,getResource,updateResource}from'../../features/resources/resourceService';
export default function ResourceForm(){const{id}=useParams(),nav=useNavigate(),[form,setForm]=useState({title:'',description:'',category:'cell_discussion' as ResourceCategory}),[file,setFile]=useState<File|null>(null),[loading,setLoading]=useState(Boolean(id)),[saving,setSaving]=useState(false),[error,setError]=useState('');useEffect(()=>{if(!id)return;getResource(id).then(r=>setForm({title:r.title,description:r.description,category:r.category})).catch(()=>setError('Resource could not be loaded.')).finally(()=>setLoading(false))},[id]);async function submit(e:React.FormEvent){e.preventDefault();if(!id&&!file){setError('Choose a file to upload.');return}if(file){const ext=file.name.split('.').pop()?.toLowerCase()??'';if(!ALLOWED_EXTENSIONS.has(ext)){setError('Choose a PDF, PowerPoint, JPG, PNG, or WebP file.');return}if(file.size>100*1024*1024){setError('Files must be 100 MB or smaller.');return}}setSaving(true);setError('');try{if(id)await updateResource(id,form);const resourceId=id??await createResource(form,file!);nav(`/admin/resources/${resourceId}`,{state:{success:id?'Resource updated.':'Upload complete.'}})}catch(err:any){setError(err?.message??'Upload failed. Please try again.');setSaving(false)}}if(loading)return <p className="res-state">Loading resource...</p>;return <div className="res-form-page"><button className="res-back" onClick={()=>nav(id?`/admin/resources/${id}`:'/admin/resources')}>← Resources</button><header><h1>{id?'Edit Resource':'Add Resource'}</h1><p>{id?'Update the resource details.':'Upload a teaching material for church leaders.'}</p></header><form className="res-panel res-form" onSubmit={submit}>{error&&<div className="res-error">{error}</div>}<label>Title *<Input required autoFocus value={form.title} onChange={e=>setForm(v=>({...v,title:e.target.value}))}/></label><label>Category *<Select value={form.category} onChange={e=>setForm(v=>({...v,category:e.target.value as ResourceCategory}))}>{Object.entries(CATEGORY_LABELS).map(([v,l])=><option key={v} value={v}>{l}</option>)}</Select></label><label>Description<Textarea rows={5} value={form.description} onChange={e=>setForm(v=>({...v,description:e.target.value}))}/></label>{!id&&<label>File *<Input required type="file" accept={ACCEPTED_RESOURCE_TYPES} onChange={e=>setFile(e.target.files?.[0]??null)}/><small>PDF, PPT, PPTX, JPG, PNG or WebP. Maximum 100 MB.</small></label>}<div className="res-actions"><Button type="button" variant="secondary" disabled={saving} onClick={()=>nav(id?`/admin/resources/${id}`:'/admin/resources')}>Cancel</Button><Button type="submit" disabled={saving}>{saving?'Uploading...':id?'Save Changes':'Upload'}</Button></div></form></div>}
